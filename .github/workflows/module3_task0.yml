name: module3_task0

on:
  push:
    branches: [ main ]
  schedule:
    - cron:  '30 17 * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Clone repositori
      run: git clone https://github.com/Oragula3/validation.git /home/john/Desktop/Curso_DevOps/Clone/

    - name: Make help
      run: make help
