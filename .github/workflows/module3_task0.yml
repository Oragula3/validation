name: module3_task0

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Make help
      working-directory: ./module3_task0
      run: |
       make help
