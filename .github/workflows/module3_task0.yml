name: module3_task0

on:
  push:
    branches: [ main ]
  schedule:
    - cron:  '30 17 * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    
    - name: Make help
      run: cd https://github.com/Oragula3/validation/tree/main/module3_task0 make help
